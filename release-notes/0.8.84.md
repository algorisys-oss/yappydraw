# 0.8.84 — Embedding doc: restrict who can frame Yappy (frame-ancestors / X-Frame-Options) (2026-07-13)

## Highlights
- Documented how to control **who is allowed to iframe Yappy at all** — the
  server-side counterpart to the control-bridge allowlist shipped in 0.8.83.

## Features
- None (docs only).

## Fixes
- None.

## Internal / tests / docs
- Help doc **Embedding** gains a "Restricting who can embed Yappy at all" section:
  `Content-Security-Policy: frame-ancestors` (allow specific parents or `'none'`)
  and the legacy `X-Frame-Options`, flagged clearly as **deploy-server config, not
  an app setting**, with a note to keep the framing allowlist and the control
  allowlist (`VITE_EMBED_ALLOWED_ORIGINS`) in sync. Search keywords updated
  (`frame-ancestors`, `X-Frame-Options`, `CSP`, `clickjacking`).

## Breaking changes / migration
- None.
