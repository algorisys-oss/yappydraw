# Env
.env
.env.local
.env.production
!.env.example
# Claude Code (track shared settings.json / mcp.json; ignore personal + runtime)
.claude/settings.local.json
.claude/*.lock

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Test results (auto-generated)
test-results

# Rust
renderers/raylib/rust/target/

# WASM build artifacts
frontend/src/wasm/build/

# Local demo render artifacts (videos/PNG) — NOT tracked (too large); regenerate via tests/*-logo|poster|variations.spec.ts
media/
.playwright-mcp/
knife-scissors-check.png
*-check.png

# Scratch space for verification screenshots, throwaway scripts and scratch output.
# Everything disposable goes here rather than at the repo root (see CLAUDE.md).
temp/
